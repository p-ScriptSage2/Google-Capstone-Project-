# AdventureMate — Multi-Agent Adventure Travel Planner

AdventureMate is a multi-agent LLM system that automates personalized adventure travel planning. It generates weather-aware, safety-informed, day-by-day itineraries, packing checklists, and provides continuous replanning and booking assistance.

## Quickstart (local demo, mocked tools)

1. Clone the repo:

```bash
git clone https://github.com/p-ScriptSage2/Google-Capstone-Project-.git
cd Google-Capstone-Project

2. Create and activate a virtual environment:
   - macOS / Linux:
     ```bash
     python3 -m venv .venv
     source .venv/bin/activate
     ```
   - Windows (PowerShell):
     ```powershell
     python -m venv .venv
     .venv\Scripts\Activate.ps1
     ```
3. Install dependencies:
```bash
pip install -r requirements.txt
